[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning:jose.*
markers =
    database: needs a reachable PostgreSQL (skipped unless TEST_DATABASE_URL is set)
